# 作業・バージョン記録

ユーザーの指示により、作業の各区切りで実務記録をこのリポジトリへ残す。
記録の入口は `doc/operations/README.md`。会話や未追跡ファイルだけに記録を残さない。

- 日時・ホスト・目的・対象 branch / commit / dirty state を記録する。
- 変更前後の version、upstream commit、依存 version、実行コマンド、終了状態、
  検証結果、判断理由、未完了事項と次の手順を記録する。
- コード変更と記録を対応付ける。記録自身を含む commit のハッシュは自己参照できないため、
  対象コード commit を明記し、記録の版は Git 履歴で追跡する。
- 別環境で必要な起動手順と実機固有の観測値を区別する。
- 短い検証ログは追跡対象として保存する。大きい成果物は保管場所・識別子・checksum を記録する。
- 未実施、失敗、暫定チェックを成功・本番検証済みと記載しない。
- ローカル commit とリモート公開の状態を区別する。公開済みと書く前に確認する。
- ホスト固有のパスは環境記録に記載し、汎用ソースコードには埋め込まない。
- 実データの存在だけを根拠に解析対象へ選ばない。試料・測定由来と用途を確認し、
  ユーザーと使用対象を合意してから実データ試験を進める。
