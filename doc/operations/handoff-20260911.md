# 次回再開用 handoff — 2026-09-11

作業 tree は clean、branch は `integration/upstream-20260911`。この handoff 作成前の HEAD は
`93db2a4`。Fork の本家統合コードは `c811f9f21a177bc97e9f21066d3585474aa880a1`。
統合前 Fork は `662864d474c87d67e351c3599e44a57debc86239`、本家は
`b4b12bd979886e50a537e28ab78ad13050776205`。統合ブランチは GitHub の Fork へ push 済み。

## 決定事項

KAMO と DIALS は module を必須にせず、ユーザー管理の共有ディレクトリへ固定配置する。
全計算ノードから同じ絶対パスで見えることを前提に、実際の Slurm ジョブで確認する。
runtime は実行中に更新しない。R / XDSSTAT / ADXV の installation check 不合格は未解消で、
合格とも扱わない。

実データは、由来・用途を確認してユーザーが指定するまで探索・解析しない。過去に由来未確認の
`/home/kuntaro/Data/images/cry00_master.h5` を先頭100画像で試験したが、正式 baseline から
除外済みで、追加処理は停止している。

## 完了事項

- 本家更新を統合し、Fork の XSCALE オプションを保持した。
- robo04 に私有 runtime を作り、`kamo --help`、Python / DIALS / KAMO / resource の参照先を確認した。
- 作業規則、環境、起動手順、クラスタ方針、停止記録を `doc/operations/` に保存した。

## 次回の開始手順

1. `git status --short --branch` と `git log -1` を確認する。
2. [startup.md](startup.md) と [cluster-environment-design.md](cluster-environment-design.md) を読む。
3. ユーザーが指定した範囲内で、クラスタの共有ディレクトリ、Slurm partition、DIALS 導入場所を調べる。
4. ユーザーが指定した検証データだけを対象にし、入力 checksum・commit・parameter・出力先を先に記録する。
5. 小規模 Slurm ジョブで `hostname`、`sys.executable`、`dials.version`、`yamtbx.__file__`、
   resource path、XDS / HDF5 plugin の参照を保存する。
6. その結果を見て、実データ baseline の実行可否を判断する。

実行前に対象データの試料・測定由来・用途・許可されたパスを確認する。不明なら解析しない。

主要な入口は [運用記録一覧](README.md) と [別環境での起動手順](startup.md)。
