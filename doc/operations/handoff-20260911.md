# 次回再開用 handoff — 2026-09-11（kuri調査後）

## Git・版

| 項目 | 値 |
| --- | --- |
| branch | `integration/upstream-20260911` |
| 同期修正前のlocal/remote HEAD | `79b1f55ffd8943c6dff1b9e9ba4f7ce209781bb5` |
| 統合コードcommit | `c811f9f21a177bc97e9f21066d3585474aa880a1` |
| 統合前Fork | `662864d474c87d67e351c3599e44a57debc86239` |
| 取り込み済み本家 | `b4b12bd979886e50a537e28ab78ad13050776205` |
| 公開状態 | 上記HEADまでGitHub Forkへpush確認済み |

本handoff自身を含むcommitは自己参照できない。再開時は次を実行し、`git log -1` と
`origin/integration/upstream-20260911` の一致を正とする。

```bash
git fetch origin
git switch integration/upstream-20260911
git pull --ff-only origin integration/upstream-20260911
git status --short --branch
git log -1 --oneline
```

## 現在の判断

- DIALS/KAMO runtimeはユーザー管理の共有・版固定配置とし、実行中に更新しない。
- 実データは試料・測定由来・用途を確認し、ユーザーが対象を指定するまで探索・解析しない。
- robo04とkuriのruntimeは別物。相互コピーや既存runtimeの変更を自動的に行わない。
- 科学的baselineは未確立。基本起動成功を実データ処理成功と扱わない。
- `origin/kmod` の段階実行機能は現統合コード・共有runtimeへ未統合。後日の別作業。

## kuriで完了したこと

- kuri04: Ubuntu 24.04.4 LTS / x86_64、Slurm 24.11.2を確認。
- `/staff` と `/user` はCephFS共有領域。
- Intel代表ノード `kuri05`、`kuri-c09` から同じ絶対パスで共有領域をread-write確認。
- `/tmp` はジョブ別node-local Btrfs領域。
- AMD系はSlurm技術試験に成功したが、正式開放は未確認。本番対象としては保留。
- 共有DIALS 3.23.0 / Python 3.11.11 / KAMO `c811f9f…` runtimeを構築。
- 当面のXDSは `xds/20260610`。login hostとIntel代表2系統で基本起動成功。
- `xds/20250714` のXDS/XSCALEは2026-08-01で期限切れ。ユーザーは作者から
  faketime利用の案内を受けており、将来別構成として導入予定。今回は未実装。
- H5ToXdsと下位コマンドは計算ノードでPATH・実行権限とも確認済み。
  合成HDF5→CBF checkは不合格で、実データ試験は未実施。

kuri固有起動:

```bash
source /staff/Common/kuntaro/kamodev/activate-kamo.sh
```

このランチャー、2.5 GBのruntime、固定checkoutはkuri共有領域にのみ存在し、GitHubには
入っていない。GitHubにはコード、再構築記録、probe、短い検証ログが入っている。

## robo04で次に行うこと

1. repositoryを最新化し、[README](README.md)、本handoff、[startup](startup.md)、
   [kuri共有runtime記録](kuri-shared-runtime-20260911.md)を読む。
2. robo04既存の独立runtimeについて、変更せずにcommit、dirty state、DIALS/Python/KAMO、
   resource pathを確認する。
3. `~/CLAUDE.md` はrobo04には存在しないとの報告がある。必須とせず、
   存在するホスト固有指示とrepositoryの `AGENTS.md` に従う。
4. ユーザーが明示するまではデータ候補を再帰探索・解析しない。
5. ユーザーと対象データの試料・測定由来・用途・許可パスを合意する。
6. 合意後、入力checksum、コードcommit、runtime/dependency version、全parameter、
   出力先を先に `doc/operations/` へ記録する。
7. robo04で小規模baselineを確立してから、同一入力・条件のkuri比較へ進む。

過去に由来未確認の `/home/kuntaro/Data/images/cry00_master.h5` を100画像で試験したが、
正式baselineから除外済み。追加処理しない。

## 主要記録

- [運用記録一覧](README.md)
- [別環境での再開手順](startup.md)
- [kuri04初期調査](kuri04-environment-survey-20260911.md)
- [kuri代表ノード調査](kuri-node-survey-20260911.md)
- [kuri共有runtime構築・検証](kuri-shared-runtime-20260911.md)
- [GitHub push記録](push-20260911.md)
