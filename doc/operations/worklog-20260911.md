# 実務記録 2026-09-11（Asia/Tokyo）

対象ホスト: robo04。対象コード: c811f9f21a177bc97e9f21066d3585474aa880a1。
各操作の正確な実行時刻は当初保存していないため、この記録は日付単位。

## 実施内容

1. 環境・既存 checkout・データ候補を読み取り調査した。
2. /oys/xtal/kundev/kamo-kun から /home/kuntaro/kamodev/kamo-merge-review に
   `git clone --no-hardlinks`。origin を Fork の HTTPS URL に設定し、upstream を追加して両方 fetch。
3. 共通祖先なしを確認し、Fork root と本家 d619df5 のツリーを比較した。
4. Fork 旧版を archive branch に保存し、integration branch を作成。
5. `git merge --allow-unrelated-histories --no-commit -s ours upstream/master` で親を設定し、
   `git read-tree --reset -u upstream/master` で本家 tree を作業用 clone に展開。
   d619df5 / origin/main / upstream/master の三者比較で5ファイルを統合した。
   この手順は初回履歴接続のためだけに実施。通常の今後の更新では繰り返さない。
6. cc_clustering の競合は本家精度と Fork コメントを残して解消。
7. 基本チェック後、統合 commit c811f9f を作成。両親は README 記載の Fork と本家。
   `git merge-base --is-ancestor upstream/master HEAD` は終了コード0、作業 tree は clean。

## 検証と未解決事項

詳細は [統合記録](../upstream-integration-20260911.md) を参照。
installation check は戻り値 False を終了コード1に変換して実行し、3項目不合格。
保存ログは [evidence](evidence/20260911-installation.txt)。
CC clustering の AST 一致、merge モジュールの import、PHIL default、XSCALE constructor の
設定切り替えを確認した。これらの確認の全 stdout は当初ファイルへ保存していないため、
実行結果の要約として記録する。科学的処理は未実施。

## 記録の整備

ユーザーの「実務記録を都度残し、別環境の startup と version 記録に使う」という指示を受け、
AGENTS.md に記録方針を追加。これまで作業ディレクトリ直下にあった調査記録と、
ignore されていた installation log を追跡対象の doc/operations/ へコピーした。
調査記録は当時の状態を保存しており、現在の状態は README を参照する。

この文書更新でソースコードや実行環境は変更していない。push は未実施。
